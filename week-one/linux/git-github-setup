# git installation

   sudo apt install git

# check version
   git --version

# git user-creation

  git config --global user.name "Your Name" # set your name

  git config --global user.email "your.email@example.com" # set your email

  git config --list # You can verify your configuration by listing all your settings

 # OR check individual setting
  
   git config --get user.name
   git config --get user.email

# GitHub Setup

  # Generate ssh keys 
   ssh-keygen -t ed25519 -C "your.email@example.com" # next pess enter ,enter, enter
# Copy your generated ssh keys from "ssh-ed25519 to email-id"

   cat ~/.ssh/id_ed25519.pub

# Next go go github profile- setting - ssh - newssh- past copy keys

  # test your ssh connection


    ssh -T git@github.com


# github test
  echo "# 90daysdevops-chalenges" >> README.md
  git init
  git add README.md
  git commit -m "first commit"
  git branch -M main
  git remote add origin git@github.com:alam-rijwan/90daysdevops-chalenges.git
  git push -u origin main

### Congratulations! You have successfully set up Git and GitHub on your Linux system.

# NOTE:
'''rijwan@devops-linux:~/Documents/90days-devops-chalenges/week-one$ git push -u origin main
To github.com:alam-rijwan/90daysdevops-chalenges.git
 ! [rejected]        main -> main (fetch first)
error: failed to push some refs to 'github.com:alam-rijwan/90daysdevops-chalenges.git'
hint: Updates were rejected because the remote contains work that you do not
hint: have locally. This is usually caused by another repository pushing to
hint: the same ref. If you want to integrate the remote changes, use
hint: 'git pull' before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.'''

# if you face above issue while pushing code to github repo, then follow below steps

  git pull origin main --rebase
  git add .
   git commit -m "your commit message"    

  git push -u origin main






 







